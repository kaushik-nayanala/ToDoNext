<Page x:Class="MainProject.View.CreateProject"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MainProject.View"
      mc:Ignorable="d" 
      Height="450" Width="800"
      Title="CreateProject" d:Background="White">

    <Canvas x:Name="MainCanvas" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid Height="{Binding ElementName=MainCanvas,Path=ActualHeight}" Width="{Binding ElementName=MainCanvas,Path=ActualWidth}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="0.5*"/>
            </Grid.RowDefinitions>
            <DockPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Label Content="Project Name"/>
                <TextBox x:Name="ProjectNameTB" MinWidth="196"/>
            </DockPanel>
            <StackPanel Grid.Row="1" HorizontalAlignment="Center">
                <Label Content="Description"/>
                <TextBox x:Name="ProjectDescriptionTB" MinHeight="200" MinWidth="300"/>
            </StackPanel>
            <Grid Grid.Row="2">
                <DockPanel HorizontalAlignment="Right">
                    <Button Content="Exit" Width="96" Height="21"/>
                    <Button Content="Save" Click="SaveClick" Width="96" Height="21"/>
                </DockPanel>
            </Grid>
        </Grid>
    </Canvas>

    <Page.Resources>
        <Style TargetType="Button">
            <Style.Triggers>
                <Trigger Property="IsPressed" Value="False">
                    <Setter Property="Template" Value="{DynamicResource NotPressed}"/>
                    <Setter Property="Margin" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <ControlTemplate x:Key="NotPressed" TargetType="Button">
            <Grid>
                <Border Background="White" BorderBrush="Black" BorderThickness="1" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}"/>
                <Label Padding="0" HorizontalAlignment="Center" VerticalAlignment="Center" Content="{TemplateBinding Content}"/>
            </Grid>
        </ControlTemplate>
    </Page.Resources>
</Page>
