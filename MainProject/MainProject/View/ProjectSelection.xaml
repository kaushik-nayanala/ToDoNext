<Window x:Class="MainProject.View.ProjectSelection"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MainProject.View"
        mc:Ignorable="d"
        Title="ProjectSelection" Height="450" Width="800">
    <Canvas x:Name="MainCanvas" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid Height="{Binding ElementName=MainCanvas,Path=ActualHeight}" Width="{Binding ElementName=MainCanvas,Path=ActualWidth}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Grid.RowSpan="2">
                <StackPanel>
                    <Button Content="New Project" Click="NewProject"/>
                    <Button Content="Open Project" Click="OpenedProject"/>
                    <Button Content="Recent Project" Click="RecentProject"/>
                    <Button Content="Exit"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Grid.Column="1" >
                <Label Content="Saved Projects"/>
            </Grid>
        </Grid>
    </Canvas>
</Window>
